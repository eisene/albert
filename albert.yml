name: albert
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - python=3.7
  - ipython
  - pip:
    - tensorflow-gpu==1.15.2
    - tensorflow-hub==0.7
    - sentencepiece
    - google-api-python-client
    - oauth2client
    - transformers
    - tqdm
    - colorama